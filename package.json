{
	"name": "patterngen",
	"displayName": "patterngen",
	"description": "RAG based code completion to enforce org wide standards",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.120.0"
	},
	"categories": [
		"AI",
		"Programming Languages",
		"Snippets"
	],
	"activationEvents": [
		"*"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": {
			"properties": {
				"patterngen.ragEndpoint": {
					"type": "string",
					"default": "http://127.0.0.1:8000",
					"description": "URL of the RAG server"
				}
			}
		},
		"commands": [
			{
				"command": "patterngen.generateCode",
				"title": "Generate Code"
			},
			{
				"command": "patterngen.openKnowledgeBase",
				"title": "Open Knowledge Base"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"pretest": "npm run compile && npm run lint",
		"lint": "eslint src",
		"test": "vscode-test"
	},
	"devDependencies": {
		"@types/mocha": "^10.0.10",
		"@types/node": "22.x",
		"@types/vscode": "^1.120.0",
		"@vscode/test-cli": "^0.0.11",
		"@vscode/test-electron": "^2.5.2",
		"eslint": "^9.39.3",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.56.1"
	},
	"dependencies": {
		"axios": "^1.16.1",
		"dotenv": "^17.4.2"
	}
}
