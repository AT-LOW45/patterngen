out
dist
node_modules
.vscode-test/
*.vsix
.env

.venv/
.vscode/settings.json

# Stray misplaced virtualenv — the real backend is rag/
src/rag/

__pycache__/
*.pyc

# Regenerable RAG artifacts — the vector store index and its dedup record manager.
# Rebuilt per-environment by (re)indexing ADRs; should not be tracked/synced via git.
rag/chroma_db/
rag/record_manager.db